/**
 * Project & Day API
 * An OpenAPI specification for project management, day templates, tasks, forms, media, and related user progress tracking.
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The Field model module.
 * @module api-content-service/models/Field
 * @version 1.0.0
 */
class Field {
    /**
     * Constructs a new <code>Field</code>.
     * @alias module:api-content-service/models/Field
     * @param fieldUuid {String} UUID for a Form Field
     * @param inputType {module:api-content-service/models/Field.InputTypeEnum} 
     * @param title {String} 
     */
    constructor(fieldUuid, inputType, title) { 
        
        Field.initialize(this, fieldUuid, inputType, title);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, fieldUuid, inputType, title) { 
        obj['field_uuid'] = fieldUuid;
        obj['input_type'] = inputType;
        obj['title'] = title;
    }

    /**
     * Constructs a <code>Field</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:api-content-service/models/Field} obj Optional instance to populate.
     * @return {module:api-content-service/models/Field} The populated <code>Field</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Field();

            if (data.hasOwnProperty('field_uuid')) {
                obj['field_uuid'] = ApiClient.convertToType(data['field_uuid'], 'String');
            }
            if (data.hasOwnProperty('input_type')) {
                obj['input_type'] = ApiClient.convertToType(data['input_type'], 'String');
            }
            if (data.hasOwnProperty('required')) {
                obj['required'] = ApiClient.convertToType(data['required'], 'Boolean');
            }
            if (data.hasOwnProperty('title')) {
                obj['title'] = ApiClient.convertToType(data['title'], 'String');
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Field</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Field</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Field.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['field_uuid'] && !(typeof data['field_uuid'] === 'string' || data['field_uuid'] instanceof String)) {
            throw new Error("Expected the field `field_uuid` to be a primitive type in the JSON string but got " + data['field_uuid']);
        }
        // ensure the json data is a string
        if (data['input_type'] && !(typeof data['input_type'] === 'string' || data['input_type'] instanceof String)) {
            throw new Error("Expected the field `input_type` to be a primitive type in the JSON string but got " + data['input_type']);
        }
        // ensure the json data is a string
        if (data['title'] && !(typeof data['title'] === 'string' || data['title'] instanceof String)) {
            throw new Error("Expected the field `title` to be a primitive type in the JSON string but got " + data['title']);
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }

        return true;
    }


}

Field.RequiredProperties = ["field_uuid", "input_type", "title"];

/**
 * UUID for a Form Field
 * @member {String} field_uuid
 */
Field.prototype['field_uuid'] = undefined;

/**
 * @member {module:api-content-service/models/Field.InputTypeEnum} input_type
 */
Field.prototype['input_type'] = undefined;

/**
 * @member {Boolean} required
 */
Field.prototype['required'] = undefined;

/**
 * @member {String} title
 */
Field.prototype['title'] = undefined;

/**
 * @member {String} description
 */
Field.prototype['description'] = undefined;





/**
 * Allowed values for the <code>input_type</code> property.
 * @enum {String}
 * @readonly
 */
Field['InputTypeEnum'] = {

    /**
     * value: "TEXT"
     * @const
     */
    "TEXT": "TEXT",

    /**
     * value: "NUMBER"
     * @const
     */
    "NUMBER": "NUMBER",

    /**
     * value: "FILE"
     * @const
     */
    "FILE": "FILE"
};



export default Field;

