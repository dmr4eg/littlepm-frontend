/**
 * Project & Day API
 * An OpenAPI specification for project management, day templates, tasks, forms, media, and related user progress tracking.
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The DayBlueprint model module.
 * @module openapi/models/DayBlueprint
 * @version 1.0.0
 */
class DayBlueprint {
    /**
     * Constructs a new <code>DayBlueprint</code>.
     * @alias module:openapi/models/DayBlueprint
     * @param dayBlueprintUuid {String} UUID for a Day Blueprint
     * @param title {String} 
     */
    constructor(dayBlueprintUuid, title) { 
        
        DayBlueprint.initialize(this, dayBlueprintUuid, title);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, dayBlueprintUuid, title) { 
        obj['day_blueprint_uuid'] = dayBlueprintUuid;
        obj['title'] = title;
    }

    /**
     * Constructs a <code>DayBlueprint</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:openapi/models/DayBlueprint} obj Optional instance to populate.
     * @return {module:openapi/models/DayBlueprint} The populated <code>DayBlueprint</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new DayBlueprint();

            if (data.hasOwnProperty('day_blueprint_uuid')) {
                obj['day_blueprint_uuid'] = ApiClient.convertToType(data['day_blueprint_uuid'], 'String');
            }
            if (data.hasOwnProperty('title')) {
                obj['title'] = ApiClient.convertToType(data['title'], 'String');
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('text')) {
                obj['text'] = ApiClient.convertToType(data['text'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>DayBlueprint</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>DayBlueprint</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of DayBlueprint.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['day_blueprint_uuid'] && !(typeof data['day_blueprint_uuid'] === 'string' || data['day_blueprint_uuid'] instanceof String)) {
            throw new Error("Expected the field `day_blueprint_uuid` to be a primitive type in the JSON string but got " + data['day_blueprint_uuid']);
        }
        // ensure the json data is a string
        if (data['title'] && !(typeof data['title'] === 'string' || data['title'] instanceof String)) {
            throw new Error("Expected the field `title` to be a primitive type in the JSON string but got " + data['title']);
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // ensure the json data is a string
        if (data['text'] && !(typeof data['text'] === 'string' || data['text'] instanceof String)) {
            throw new Error("Expected the field `text` to be a primitive type in the JSON string but got " + data['text']);
        }

        return true;
    }


}

DayBlueprint.RequiredProperties = ["day_blueprint_uuid", "title"];

/**
 * UUID for a Day Blueprint
 * @member {String} day_blueprint_uuid
 */
DayBlueprint.prototype['day_blueprint_uuid'] = undefined;

/**
 * @member {String} title
 */
DayBlueprint.prototype['title'] = undefined;

/**
 * @member {String} description
 */
DayBlueprint.prototype['description'] = undefined;

/**
 * @member {String} text
 */
DayBlueprint.prototype['text'] = undefined;






export default DayBlueprint;

